// A fixture for ImportBoundaryTests. The package build excludes this file.
// The ACP target can import the ACP packages. FoundationModelsACP is not
// FoundationModels, so the scanner must not report these lines.
// import FoundationModels is in a comment, so the scanner must not report it.
import FoundationModelsACP
import FoundationModelsACPClient
import SwiftUI
