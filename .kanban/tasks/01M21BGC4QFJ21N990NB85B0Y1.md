---
comments:
- actor: claude-code
  id: 01m2pv93k5xawce6e7nn010qx7
  text: |-
    ### finish iteration 1 — clean

    - implement: wrote the R6 survey and the decision (v2 only) in Docs/decisions/acp-version.md. Updated plan.md decision 20, §3.3 and R6. Added SupportedProtocolVersions, ACPThreadSource.initialize(over:request:) and acceptProtocolVersion(_:requested:). Added ProtocolVersionTests (9 tests). The ACP test target now links PackageFileSupport.
    - test: swift test exit 0. AgentViewKitTests 1044, AgentViewKitACPTests 102 (was 93), AgentViewKitRouterTests 71, PackageFileSupportTests 22, AgentViewKitFoundationModelsTests 44. Only the accepted mlx-swift warnings.
    - commit: 7d3d292 feat(acp): record the R6 ACP version decision and refuse an unsupported version (^b85b0y1)
    - review: review sha HEAD~1..HEAD. Zero findings (1 candidate refuted). No validator matches the two Markdown files.
  timestamp: 2026-09-17T04:51:26.437488+00:00
depends_on:
- 01M21ADNAKRK96TKZQMRMW1MG7
position_column: done
position_ordinal: bb80
title: 'Research R6: which agents speak ACP v1 and v2, and the version the kit negotiates (plan §14)'
---
## What
Settle research R6 from plan.md §14 and record it where the code can check it.

- Survey the ACP versions spoken today by Claude Code's ACP adapter, Codex, Gemini CLI, Zed, and Xcode 27, from their docs and changelogs. Record the table with URLs and dates in `Docs/decisions/acp-version.md`.
- Decide: v2 only, or v2 plus a v1 adapter. Write the decision and the reason in the same file, and update plan.md decision 20.
- Encode the decision: `AgentViewKitACP.SupportedProtocolVersions` lists the integers the kit accepts. `ACPThreadSource` refuses a session whose negotiated version is not in the list with a `.error` item that names both versions.

## Acceptance Criteria
- [x] `Docs/decisions/acp-version.md` exists with the table and the decision.
- [x] `SupportedProtocolVersions` matches the decision (a test parses the decision file's `supported:` line and compares).
- [x] A negotiated version outside the list produces one `.error` item.

## Tests
- [x] `Tests/AgentViewKitACPTests/ProtocolVersionTests.swift`: the decision-file match and the refusal path over the in-memory agent.
- [x] `swift test --filter AgentViewKitACPTests` exits 0.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass.

## Review Findings (2026-09-16 23:46)

Scope: `review sha HEAD~1..HEAD` (commit 7d3d292). 4 files reviewed. Zero findings (1 candidate refuted). No validator matches `Docs/decisions/acp-version.md` or `plan.md`.